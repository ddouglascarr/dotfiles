#!/bin/bash

################################################################################
#
# set-esp-env
#
#   source this to set env variables fpr esp32 development
#   good idea to add it to the virtualenv postactivate
#
################################################################################

export PATH="$HOME/src/esp/xtensa-esp32-elf/bin:$PATH"
export IDF_PATH="$HOME/src/esp/esp-idf"
